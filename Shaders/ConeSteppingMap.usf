
for(int i = 0; i < 64; i++)
{
	for(int j = 0; j < 256; j++)
	{
		Texture2DSample(ReliefMap, ReliefMapSampler, UV) + 0.01;
	}
}
return Texture2DSample(ReliefMap, ReliefMapSampler, UV) + 0.01;
